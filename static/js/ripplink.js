$(function () { var b, c, a, e; $(".ripplelink").click(function (d) { if ($(this).find(".ink").length === 0) { $(this).prepend("<span class='ink'></span>") } b = $(this).find(".ink"); b.removeClass("animate"); if (!b.height() && !b.width()) { c = Math.max($(this).outerWidth(), $(this).outerHeight()); b.css({ height: c, width: c }) } a = d.pageX - $(this).offset().left - b.width() / 2; e = d.pageY - $(this).offset().top - b.height() / 2; b.css({ top: e + "px", left: a + "px" }).addClass("animate") }); $(".ripplelink2").click(function (d) {  if (!b.height() && !b.width()) { c = Math.max($(this).outerWidth(), $(this).outerHeight()); b.css({ height: c, width: c }) } a = d.pageX - $(this).offset().left - b.width() / 2; e = d.pageY - $(this).offset().top - b.height() / 2; b.css({ top: e + "px", left: a + "px" }).addClass("animate") }) });